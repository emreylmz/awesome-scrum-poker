import React from 'react'

type DoneCallback = jest.DoneCallback

describe('View Planing As Developer Page UI', (): void => {
  it('should render correctly', (done: DoneCallback): void => {
    done()
  })

  it('should render StoryList component', (done: DoneCallback): void => {
    done()
  })

  it('should render ActiveStory component', (done: DoneCallback): void => {
    done()
  })
})

describe('View Planing As Developer Page Logic', (): void => {
  it('should vote active story', (done: DoneCallback): void => {
    done()
  })
})
