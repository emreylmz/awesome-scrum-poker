type DoneCallback = jest.DoneCallback

describe('View Planing As Scrum Master Page UI', (): void => {
  it('should render correctly', (done: DoneCallback): void => {
    done()
  })

  it('should show StoryList component', (done: DoneCallback): void => {
    done()
  })

  it('should show ActiveStory component', (done: DoneCallback): void => {
    done()
  })

  it('should show ScrumMasterPanel component', (done: DoneCallback): void => {
    done()
  })

  it('should update Scrum Master Point when a story point selected', (done: DoneCallback): void => {
    done()
  })

  it('should update active story panel every 2 second with get voters', (done: DoneCallback): void => {
    done()
  })

  it('should update story list panel every 2 seconds', (done: DoneCallback): void => {
    done()
  })
})

describe('View Planing As Scrum Master Page Logic', (): void => {
  it('should get session name from url', (done: DoneCallback): void => {
    done()
  })

  it('should get session information from server', (done: DoneCallback): void => {
    done()
  })

  it('should redirect to AddStoryListPage when there is no session name parameter in url', (done: DoneCallback): void => {
    done()
  })

  it('should be able to continue to next story', (done: DoneCallback): void => {
    done()
  })

  it('should send story point to server when before continue to next story', (done: DoneCallback): void => {
    done()
  })
})
