import React from 'react'

type DoneCallback = jest.DoneCallback

describe('Add Story List Page UI', (): void => {
  it('should render correctly', (done: DoneCallback): void => {
    done()
  })

  it('should render session name labeled text box component', (done: DoneCallback): void => {
    done()
  })

  it('should render number of voters labeled text box component', (done: DoneCallback): void => {
    done()
  })

  it('should render story list labeled text area component', (done: DoneCallback): void => {
    done()
  })
})

describe('Add Story List Page Logic', (): void => {
  it('should not continue when Start Sprint button clicked if required areas are empty', (done: DoneCallback): void => {
    done()
  })

  it('should send startSession request when start session button button clicked', (done: DoneCallback): void => {
    done()
  })

  it('should send required parameters to startSession service', (done: DoneCallback): void => {
    done()
  })

  it('should not continue when session name length is more than 200 character or empty', (done: DoneCallback): void => {
    done()
  })

  it('should redirect to ViewPlaningAsScrumMaster page when start session button clicked', (done: DoneCallback): void => {
    done()
  })

  it('should not continue when value of number of voters is empty or 0', (done: DoneCallback): void => {
    done()
  })

  it('should accept each story list line as a story', (done: DoneCallback): void => {
    done()
  })
})
